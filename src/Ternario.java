public class Ternario {
    
    public static void main(String[] args) {
        
        double media = 7.0;
        String resultado = media >= 7.0 ? "aprovado." : "em recuperação.";

        System.err.println("O aluno está: " + resultado);
    }
}
